#include "sec_log.h"
#include <stdio.h>

void sec_log_init(void)
{
	//later: file logging
}
void sec_log_event(const chat *msg, const frame_t *f)
{
	printf("[SEC] %s: PGN=0x%06X SA=0x%02", msg, f->pgn, g->sa, f->len);

}
